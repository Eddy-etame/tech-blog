# Tech Blog

## Development

Use the URL shown by `symfony server:start` (typically http://127.0.0.1:8001).

Do not use port 8000 unless you have configured it—a different server may run there and assets may fail to load.
